apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mini-finance-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"     # ✅ String with quotes
    alb.ingress.kubernetes.io/scheme: "internet-facing"  # ✅ String with quotes
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mini-finance-service
                port:
                  number: 80